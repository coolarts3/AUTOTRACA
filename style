/* --- Variables de Color (Paleta Clásica Racing) --- */
:root {
    --primary-color: #FF0000; /* Rojo Intenso */
    --secondary-color: #1E1E1E; /* Gris muy oscuro / Casi Negro */
    --background-light: #FFFFFF; /* Blanco */
    --background-dark: #111111; /* Negro para secciones oscuras */
    --text-light: #F8F9FA; /* Texto claro sobre fondos oscuros */
    --text-dark: #212529; /* Texto oscuro sobre fondos claros */
    --accent-color: #C0C0C0; /* Plata/Gris claro para detalles */
}

/* --- Reset Básico y Estilos Globales --- */
* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}

body {
    font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif; /* O una fuente más racing si tienes */
    line-height: 1.6;
    color: var(--text-dark);
    background-color: var(--background-light);
}

.container {
    width: 90%;
    max-width: 1100px;
    margin: 0 auto;
    padding: 0 20px;
}

h1, h2, h3 {
    margin-bottom: 1rem;
    color: var(--primary-color); /* Títulos principales en rojo */
}

h2 {
    font-size: 2rem;
    text-align: center;
    padding-top: 1.5rem;
}

p {
    margin-bottom: 1rem;
}

a {
    text-decoration: none;
    color: var(--primary-color);
}

a:hover {
    text-decoration: underline;
}

img {
    max-width: 100%;
    height: auto;
}

/* --- Header y Navegación --- */
header {
    background-color: var(--secondary-color);
    color: var(--text-light);
    padding: 1rem 0;
    position: sticky; /* Para que se quede fijo arriba (opcional) */
    top: 0;
    z-index: 1000;
    box-shadow: 0 2px 5px rgba(0,0,0,0.2);
}

.header-content {
    display: flex;
    justify-content: space-between;
    align-items: center;
}

.logo img {
     height: 50px; /* Ajusta según tu logo */
}
/* Si usas texto para el logo */
/*
.logo h1 {
    color: var(--text-light);
    margin: 0;
    font-size: 1.8rem;
}
.logo h1 a { color: inherit; text-decoration: none; }
*/

nav ul {
    list-style: none;
    display: flex;
}

nav ul li {
    margin-left: 20px;
}

nav ul li a {
    color: var(--text-light);
    text-transform: uppercase;
    font-weight: bold;
    padding: 5px 10px;
    border-radius: 4px;
    transition: background-color 0.3s ease;
}

nav ul li a:hover, nav ul li a.active { /* 'active' necesitaría JS para funcionar bien en SPA */
    background-color: var(--primary-color);
    color: var(--background-light);
    text-decoration: none;
}

/* --- Sección Héroe --- */
#hero {
    position: relative; /* Necesario para el overlay */
    height: 70vh; /* Altura del banner */
    background: url('images/hero-banner-placeholder.jpg') no-repeat center center/cover; /* REEMPLAZA con tu imagen */
    display: flex;
    align-items: center;
    justify-content: center;
    text-align: center;
    color: var(--text-light);
}

.hero-overlay { /* Capa oscura opcional para legibilidad */
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background-color: rgba(0, 0, 0, 0.5); /* Ajusta la opacidad */
    z-index: 1;
}

.hero-content {
    position: relative; /* Para que esté sobre el overlay */
    z-index: 2;
}

#hero h1 {
    font-size: 3.5rem;
    margin-bottom: 1rem;
    color: var(--text-light); /* Título del héroe en blanco */
}

#hero p {
    font-size: 1.2rem;
    margin-bottom: 2rem;
}

.cta-button { /* Botón Call-to-Action Principal */
    display: inline-block;
    background-color: var(--primary-color);
    color: var(--background-light);
    padding: 12px 25px;
    font-size: 1.1rem;
    font-weight: bold;
    border: none;
    border-radius: 5px;
    text-transform: uppercase;
    transition: background-color 0.3s ease, transform 0.2s ease;
}

.cta-button:hover {
    background-color: #cc0000; /* Rojo más oscuro al pasar el ratón */
    text-decoration: none;
    transform: scale(1.05);
}

.cta-button-secondary { /* Botón secundario */
    display: inline-block;
    background-color: transparent;
    color: var(--primary-color);
    padding: 10px 20px;
    border: 2px solid var(--primary-color);
    border-radius: 5px;
    font-weight: bold;
    transition: background-color 0.3s ease, color 0.3s ease;
}

.cta-button-secondary:hover {
    background-color: var(--primary-color);
    color: var(--background-light);
    text-decoration: none;
}


/* --- Secciones de Contenido --- */
.content-section {
    padding: 4rem 0;
    text-align: center;
}

.content-section h2 {
    margin-bottom: 2rem;
}

.dark-section {
    background-color: var(--background-dark);
    color: var(--text-light);
}

.dark-section h2 {
     color: var(--primary-color); /* Mantenemos títulos en rojo */
}

.dark-section a {
    color: var(--accent-color); /* Enlaces más claros en fondo oscuro */
}
.dark-section a:hover {
     color: var(--primary-color);
}

.dark-section .cta-button-secondary {
    color: var(--accent-color);
    border-color: var(--accent-color);
}
.dark-section .cta-button-secondary:hover {
    background-color: var(--accent-color);
    color: var(--background-dark);
}


/* --- Sección Noticias (Grid Ejemplo) --- */
.news-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(280px, 1fr)); /* Columnas responsivas */
    gap: 2rem;
    text-align: left;
    margin-top: 2rem;
}

.news-item {
    background-color: var(--secondary-color); /* Fondo para cada tarjeta de noticia */
    padding: 1.5rem;
    border-radius: 8px;
    box-shadow: 0 4px 8px rgba(0,0,0,0.1);
    transition: transform 0.3s ease;
}

.news-item:hover {
    transform: translateY(-5px);
}

.news-item img.news-image {
    width: 100%;
    height: 180px; /* Altura fija para imágenes */
    object-fit: cover; /* Para que la imagen cubra sin deformarse */
    border-radius: 4px;
    margin-bottom: 1rem;
}

.news-item h3 {
    color: var(--primary-color);
    margin-bottom: 0.5rem;
}

.news-item p {
    font-size: 0.9rem;
    margin-bottom: 1rem;
    color: var(--text-light); /* Texto claro en tarjetas oscuras */
}

.news-item a {
    font-weight: bold;
}

/* --- Sección Pilotos (Preview Ejemplo) --- */
.driver-preview {
    margin: 2rem 0;
    display: flex;
    justify-content: center;
    gap: 1rem;
}
.driver-preview span { /* Reemplazar span con img cuando tengas fotos */
    display: inline-block;
    width: 80px;
    height: 80px;
    background-color: var(--accent-color);
    color: var(--secondary-color);
    border-radius: 50%; /* Círculo */
    line-height: 80px; /* Centrar texto verticalmente (si usas texto) */
    text-align: center;
    font-weight: bold;
}

/* --- Sección Contacto --- */
#contact .social-links {
    margin-top: 1.5rem;
}

#contact .social-links a {
    margin: 0 10px;
    font-size: 1.5rem; /* Tamaño de los iconos (ajustar si usas font icons o SVGs) */
    color: var(--accent-color);
    text-decoration: none;
    transition: color 0.3s ease;
}

#contact .social-links a:hover {
    color: var(--primary-color);
}

/* --- Footer --- */
footer {
    background-color: var(--secondary-color);
    color: var(--text-light);
    text-align: center;
    padding: 1.5rem 0;
    margin-top: 2rem; /* Separación del último contenido */
}

footer p {
    margin: 0;
    font-size: 0.9rem;
}

footer a {
    color: var(--accent-color);
}
footer a:hover {
    color: var(--primary-color);
}

/* --- Media Queries (Ejemplo Básico para Móviles) --- */
@media (max-width: 768px) {
    .header-content {
        flex-direction: column;
        text-align: center;
    }

    nav ul {
        flex-direction: column;
        margin-top: 1rem;
    }

    nav ul li {
        margin: 10px 0;
    }

    #hero h1 {
        font-size: 2.5rem;
    }

    #hero p {
        font-size: 1rem;
    }

    .content-section {
        padding: 2rem 0;
    }

     h2 {
        font-size: 1.8rem;
    }
}
